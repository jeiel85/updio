name: Build and Release

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]
  workflow_dispatch:

jobs:
  build:
    permissions:
      contents: write
    strategy:
      fail-fast: false
      matrix:
        platform: [windows-latest]

    runs-on: ${{ matrix.platform }}
    steps:
      - uses: actions/checkout@v4

      - name: setup node
        uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: 'npm'

      - name: install Rust stable
        uses: dtolnay/rust-toolchain@stable

      - name: setup python
        uses: actions/setup-python@v5
        with:
          python-version: '3.10'

      - name: install python dependencies
        run: |
          python -m pip install --upgrade pip
          pip install pyinstaller

      - name: install node dependencies
        run: npm install

      - name: build sidecar
        run: npm run build:sidecar

      - name: download ffmpeg sidecar
        shell: pwsh
        run: |
          $ffmpeg_url = "https://github.com/GyanD/codexffmpeg/releases/download/7.1/ffmpeg-7.1-full_build.zip"
          Invoke-WebRequest -Uri $ffmpeg_url -OutFile ffmpeg.zip
          Expand-Archive ffmpeg.zip -DestinationPath ffmpeg_temp
          $bin_dir = "src-tauri/binaries"
          if (!(Test-Path $bin_dir)) { New-Item -ItemType Directory -Path $bin_dir }
          Copy-Item "ffmpeg_temp/*/bin/ffmpeg.exe" -Destination "$bin_dir/ffmpeg-x86_64-pc-windows-msvc.exe"
          Copy-Item "ffmpeg_temp/*/bin/ffprobe.exe" -Destination "$bin_dir/ffprobe-x86_64-pc-windows-msvc.exe"

      - name: build tauri app
        uses: tauri-apps/tauri-action@v0
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        with:
          tagName: v__VERSION__
          releaseName: "Vibe Video Upscaler v__VERSION__"
          releaseBody: "Automated release from GitHub Actions. Includes both installer (.msi) and standalone (.exe) versions."
          releaseDraft: false
          prerelease: false
